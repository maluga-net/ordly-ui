i1, i2, i3

i4

get strategy type from list
get strategy object using factory
pass existing items and new item to strategy object
trigger event processStrategy passing strategy object

----

strategyView object listens for  processStrategy event on HierarchizedLists
runs processStrategy method
if strategy is completed then trigger event addItem with item data (name + position)
if strategy is not completed fetch Selection object, render selection template with it and store locally in the view

----

user clicks one of items in selection template
selectFirst/selectSecond method is triggered which calls selectFirst/selectSecond method on strategy
and triggers event processStrategy passing strategy object 
